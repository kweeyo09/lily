export { COOKIE_NAME, ONE_YEAR_MS } from "@shared/const";

/**
 * Where to send visitors who hit an authenticated route while signed out.
 * No identity provider is wired up yet; point this at your sign-in page once
 * one exists.
 */
export const LOGIN_PATH = "/";
